<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <%= stylesheet_link_tag 'styles', media: 'all', 'data-turbo-track': 'reload' %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Racing+Sans+One&family=Sacramento&family=Shadows+Into+Light&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <script src="./script.js" defer></script>
  <title>Euro Central</title>
</head>
<body class="bg-dark text-light">
  <div class="container mt-3">
    <h1 class="headings">All Blogs</h1>
    <%= link_to 'New Post', new_post_path, class: 'btn btn-primary mb-3' %>
    <div class="row flex-row flex-nowrap overflow-auto">
      <% Post.all.each do |post| %>
        <div class="col-3">
          <div class="card bg-dark text-light mb-3">
            <div class="card-header">
              <%= post.title %>
            </div>
            <div class="card-body">
              <p class="card-text"><%= truncate_html(post.body.html_safe, length: 60) %></p>
              <%= link_to 'View Post', post_path(post), class: 'btn btn-primary' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <h1 class="headings mt-5">All Listings</h1>
    <%= link_to 'New Listing', new_listing_path, class: 'btn btn-primary mb-3' %>
    <div class="row flex-row flex-nowrap overflow-auto">
      <% Listing.all.each do |listing| %>
        <div class="col-3">
          <div class="card bg-dark text-light mb-3">
            <div class="card-header">
              <%= listing.title %>
            </div>
            <div class="card-body">
              <% if listing.image.present? %>
                <%= image_tag listing.image, class: 'card-img-top img-fluid', alt: listing.title %>
              <% end %>
              <p class="card-text"><%= truncate_html(listing.body.html_safe, length: 60) %></p>
              <%= link_to 'View Listing', listing_path(listing), class: 'btn btn-primary' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <h1 class="headings mt-5">All Documents</h1>
    <%= link_to 'New Document', new_document_path, class: 'btn btn-primary mb-3' %>
    <div class="row flex-row flex-nowrap overflow-auto">
      <% Document.all.each do |document| %>
        <div class="col-3">
          <div class="card bg-dark text-light mb-3">
            <div class="card-header">
              <%= document.name %>
            </div>
            <div class="card-body">
              <% if document.document.attached? %>
                <%= link_to truncate_html(document.document.filename.to_s, length: 30), rails_blob_path(document.document, disposition: "attachment"), class: 'text-white text-decoration-none' %>
              <% else %>
                <span class="text-muted">No file attached</span>
              <% end %>
              <%= link_to 'Preview', '#', data: { bs_toggle: 'modal', bs_target: "#documentModal#{document.id}" }, class: 'btn btn-secondary mt-2' %>
              <%= link_to 'Delete', document_path(document), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger mt-2' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Modal for document preview -->
    <% Document.all.each do |document| %>
      <div class="modal fade" id="documentModal<%= document.id %>" tabindex="-1" aria-labelledby="documentModalLabel<%= document.id %>" aria-hidden="true">
        <div class="modal-dialog modal-lg">
          <div class="modal-content bg-dark text-light">
            <div class="modal-header">
              <h5 class="modal-title" id="documentModalLabel<%= document.id %>"><%= document.name %></h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
              <% if document.document.attached? %>
                <iframe src="<%= rails_blob_path(document.document, disposition: "inline") %>" width="100%" height="500px"></iframe>
              <% else %>
                <p class="text-muted">No file attached</p>
              <% end %>
            </div>
            <div class="modal-footer">
              <% if document.document.attached? %>
                <%= link_to 'Download', rails_blob_path(document.document, disposition: "attachment"), class: 'btn btn-primary' %>
              <% end %>
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <script src="./script.js" defer></script>
</body>
</html>